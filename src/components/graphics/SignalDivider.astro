---
interface Props {
	label: string;
	animate?: boolean;
}

const { label, animate = false } = Astro.props;
---

<div class:list={["signal-divider", { "animate-scan": animate }]} aria-hidden="true">
	<span>{label}</span>
</div>
